// import { notFound } from "next/navigation";
// import { getRequestConfig } from "next-intl/server";

// Can be imported from a shared config
const locales = ["en", "ar"];

// Simple i18n config for now - not using next-intl
export default {
  locales,
  defaultLocale: "en"
};
